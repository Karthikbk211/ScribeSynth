DATA_LOADER:
  IMAGE_PATH: 'data/images'
  STYLE_PATH: 'data/style'
  FREQ_PATH: 'data/freq'
  NUM_THREADS: 4

MODEL:
  IN_CHANNELS: 4
  OUT_CHANNELS: 4
  EMB_DIM: 256
  NUM_RES_BLOCKS: 2
  NUM_HEADS: 8

TRAIN:
  SEED: 42
  IMS_PER_BATCH: 4
  TYPE: 'train'

TEST:
  IMS_PER_BATCH: 2
  TYPE: 'test'

SOLVER:
  BASE_LR: 0.00001

OUTPUT_DIR: 'output/finetune'
